# Performing maths with answers

print("What number would you like to multiply by 10?")
spam = input() # Get the input from the user

print("The answer is " + str(int(spam) * 10)) # Turn the input into an integer so that it can be multiplied by 10